@model IEnumerable<ChitChat.Models.Tweet>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TweetMessage)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TweetMessageTime)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TweetMessage)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TweetMessageTime)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TweetId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TweetId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TweetId })
        </td>
    </tr>
}

</table>
